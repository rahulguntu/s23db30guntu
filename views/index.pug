extends layout

block content 
  //- a(href='costume') costume
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/costume") Take me to costume
    br
    a(href="/logout") Logout